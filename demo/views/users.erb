<!doctype html>
<html>
  <head>
    <link href="../css/custom.css" type="text/css" rel="stylesheet"/>
  </head>
  <body>
    <h2>Users</h2>
    <%= @users %>
    <% @users.each do |user| %>
      <p><%= user[1]["name"] %>, <%= user[1]["email"] %></p>
    <% end %>
  </body>
</html>